import OnboardingPage from '@/app/onboarding/page';

export const metadata = {
  title: 'Carrier Setup & Onboarding | OTR Dispatch',
  description: 'Fast-track your carrier onboarding packet with OTR Dispatch. Upload MC Authority, Certificate of Insurance, and W-9 form for instant dispatch representation.',
};

export default function CarrierSetupPage() {
  return <OnboardingPage />;
}
